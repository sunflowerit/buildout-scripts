# Buildout Scripts Odoo

These scripts can be developed and used inside of an Odoo buildout recipe.

### Regenerating the package files

After modifying something in this package, run:

```
python setup.py sdist
```
